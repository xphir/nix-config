{...}: {
  programs = {
    # terminal file manager
    yazi = {
      enable = true;
      settings = {
        mgr = {
          show_hidden = true;
          sort_dir_first = true;
        };
      };
    };
  };
}
